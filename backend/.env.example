# 服务器配置
ENV=development
PORT=8080

# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=promeconfig
DB_SSL_MODE=disable

# JWT配置
JWT_SECRET=your-secret-key
JWT_EXPIRATION=24h

# Prometheus配置
PROMETHEUS_URL=http://localhost:9090
PROMETHEUS_USERNAME=
PROMETHEUS_PASSWORD=